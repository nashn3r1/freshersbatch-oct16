export function testtemp()
{
    var model='HP 2236 tx';
    var name="Ninad";
    var deskno = "x5222";
    let message = `My name is '${name}' and my desk number is ${deskno}.
    I am having problem with my laptop whose model number is ${model}. `
    console.log(message);
}